version: "3.9"

services:
  ai-league:
    build:
      context: .
      dockerfile: Dockerfile
    image: ai-league:latest
    ports:
      - "8080:8000"
    environment:
      FRONTEND_DIST_PATH: /app/frontend-dist
    restart: unless-stopped
    # 添加网络配置以提高构建成功率
    network_mode: "bridge"